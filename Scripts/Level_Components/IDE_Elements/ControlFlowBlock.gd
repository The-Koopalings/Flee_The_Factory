extends "res://Scripts/Level_Components/IDE_Elements/FunctionBlock.gd"
#Make sure the highlight functions still work if we extend rather than put it here directly


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
